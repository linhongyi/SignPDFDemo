import { SignatureRectangle } from './signature-rectangle';

describe('SignatureRectangle', () => {
  it('should create an instance', () => {
    expect(new SignatureRectangle(0,0,0,0)).toBeTruthy();
  });
});
